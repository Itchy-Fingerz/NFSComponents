//
//  Theme.swift
//  NFSComponents
//

import SwiftUI

/// Namespace for the NFSComponents design tokens (colors, typography, spacing,
/// radii and shadows). All tokens are self-contained — the package ships its
/// own palette and fonts and does not read from a host-app asset catalog.
public enum Theme {}
